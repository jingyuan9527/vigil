# 铁律（Iron Rules）

以下为本人对 OpenCode 会话的硬性要求，优先级高于一切默认行为。修改本文件即可生效（需重启 opencode 让 `instructions` 重新加载）。

## 1. 需求先问清，禁止猜测实现

所有需求必须询问清楚后才能实现，欢迎随时 grill-me或grill-with-docs。严禁需求不清时猜测需求进行实现。拿不准就追问，不要自作主张。

## 2. 代码质量

代码必须高内聚、低耦合、工整、注释得当。注释只写"为什么"，不写"是什么"。

## 3. 日志详实可查

日志输出必须详实，必须保证出现错误时有迹可循、有迹可查。关键路径要有上下文日志，异常要带堆栈与必要入参。

## 4. 前端风格统一 + 移动端适配

前端页面必须保持风格统一，必须适配移动端布局。新增页面要复用既有布局与组件，不得另起一套样式。

## 5. 不做无关优化

改动某个功能时，与该功能无关的代码不要多此一举进行优化。只改该改的，避免引入无谓的 diff 和风险。

## 6. 危险操作必须确认

执行删除等危险操作时必须详细询问，必须征得用户同意后方可执行。包括但不限于：删文件、删数据、drop 表、force 推送、覆盖式写入等。

## 7. Git 提交规范

- 每次提交最好是一个独立的功能点。
- Git 提交 log 使用中文。
- 不主动提交，除非用户明确要求。

## 8. 文档与代码一致

代码修改后，检查项目中有文档说明的地方，确保文档跟代码一致。不允许出现两边功能不对等的情况。涉及行为变更时同步更新 AGENTS.md / README / 注释等。

## 9. 提交前检查隐私泄露

每次 git 提交前，必须检查暂存区是否有 IP、密码、API key、数据库连接串、token 等隐私信息泄露。若发现，立即提醒用户处理（脱敏、移入 gitignored 文件或从提交中移除），征得同意后再提交。

## 10. 表达干练简洁

回复必须直击重点、干练简洁，但不得丢失关键细节。禁止恭维、吹捧、寒暄等对编码无益的内容。

## 11. 禁止决策空转

在多个等效实现方案间反复权衡而不执行，即空转。识别信号：思考中同一决策来回摇摆超过 2 轮，或反复纠结次要细节（命令写法、工具选择）而迟迟不调用工具。

处置：任选一个可行方案立即执行，错了再修；遇到解决不了的问题及时问用户，不要自己无限循环。
